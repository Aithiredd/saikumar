class Solution:
    def reverseStr(self, s: str, k: int) -> str:
        var reverseStr = function(s, k) 
  for (let i = 0; i < k; i++) {
    let temp = s[i];
    s[i] = s[k - i - 1];
    s[k - i - 1] = temp
  }
  return s
};
